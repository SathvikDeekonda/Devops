name: Build and Notify

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # 1️⃣ Notify build start
      - name: Notify Build Start
        run: |
          echo "Sending 'build started' notification..."
          curl -X POST https://avril-parallelepipedous-unlethally.ngrok-free.dev/notify \
            -H "Content-Type: application/json" \
            -d "{
              \"repo\": \"${{ github.repository }}\",
              \"status\": \"started\",
              \"branch\": \"${{ github.ref_name }}\",
              \"pusher\": \"${{ github.actor }}\",
              \"commit_message\": \"${{ github.event.head_commit.message }}\",
              \"commit_id\": \"${{ github.event.head_commit.id }}\"
            }"

      - name: Simulate build
        run: |
          echo "Building project..."
          sleep 5  # simulate build time

      # 2️⃣ Notify build completion
      - name: Notify Build Completion
        if: always()
        run: |
          echo "Sending 'build completed' notification..."
          curl -X POST https://avril-parallelepipedous-unlethally.ngrok-free.dev/notify \
            -H "Content-Type: application/json" \
            -d "{
              \"repo\": \"${{ github.repository }}\",
              \"status\": \"${{ job.status }}\",
              \"url\": \"https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}\",
              \"branch\": \"${{ github.ref_name }}\",
              \"pusher\": \"${{ github.actor }}\",
              \"commit_message\": \"${{ github.event.head_commit.message }}\",
              \"commit_id\": \"${{ github.event.head_commit.id }}\"
            }"
